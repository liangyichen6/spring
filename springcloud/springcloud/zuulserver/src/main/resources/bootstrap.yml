spring:
    application:
        name: zuulserver
    #config server configuration
    cloud:
        config:
            discovery: 
                enabled: true
                serviceId: configserver
                


    